# 马卡龙提醒 🍬（MVP）

Mac 桌面右侧滑入的可爱提醒小应用。糖果色马卡龙风格，常驻菜单栏，到点弹窗提醒。

## 怎么运行

```bash
# 开发模式（改代码即时热更新，首次编译 Rust 较慢，耐心等）
pnpm tauri dev

# 打包成 .app / .dmg（产物在 src-tauri/target/release/bundle/）
pnpm tauri build
```

运行后**没有主窗口**，只在屏幕顶部菜单栏出现一个图标。点它有菜单：

- **测试提醒一下** —— 立刻弹一个弹窗，验证效果（不用等到点）
- **开启提醒声音** —— 勾选/取消，控制是否播放提示音
- **开机自动启动** —— 勾选后随系统登录自动启动（打包安装后生效最佳）
- **退出** —— 关闭应用

## 怎么改提醒内容

提醒都写在这个文件里，用任意编辑器改完保存即可，**≤20 秒自动生效，无需重启**（含 `soundEnabled` 开关）：

```
~/.MacaBell/reminders.json
```

首次运行会自动生成示例。三种提醒类型：

```jsonc
{
  "soundEnabled": true,          // 全局声音开关
  "reminders": [
    {
      "id": "work-report",       // 唯一标识，随便起，别重复
      "type": "daily",           // ① 每天固定时间点
      "time": "14:00",           // 24 小时制 HH:MM
      "title": "工作提醒",
      "message": "该写周报啦"
    },
    {
      "id": "sit-too-long",
      "type": "interval",        // ② 固定间隔循环
      "everyMinutes": 60,        // 每隔多少分钟提醒一次（从应用启动算起）
      "title": "久坐提醒",
      "message": "起来动动，喝口水吧"
    },
    {
      "id": "anniversary",
      "type": "anniversary",     // ③ 每年某月某日
      "date": "06-15",           // MM-DD
      "time": "09:00",
      "title": "纪念日",
      "message": "今天是个特别的日子"
    }
  ]
}
```

> 想加提醒就往 `reminders` 数组里加一项；想停某条就删掉它。

## 验证好不好用的最快路径

1. `pnpm tauri dev` 起来
2. 点菜单栏图标 → **测试提醒一下** → 右上角应滑入糖果色弹窗
3. 想测真实调度：把某条 `daily` 的 `time` 改成当前时间 +1 分钟，保存等它弹（≤20s 加载新配置，不用重启）

## 这个 MVP 没做（验证好用后再加）

- 增删提醒的图形界面（现在直接改 JSON）
- 自定义声音、勿扰时段、提醒历史
- 开机自启

## 技术栈

Tauri 2 + React + TypeScript。后端（调度/托盘/声音/窗口）在 `src-tauri/src/lib.rs`，弹窗 UI 在 `src/App.tsx` + `src/App.css`。
