# MacaBell · 产品文档

> 版本：v0.2（开源可用性版）｜更新：2026-06-08
> 定位结论先行：**现在是一个本地优先的 Mac 菜单栏提醒工具，长期方向是「面向 AI coding workflow 的本地任务收件箱」。** 全程保持本地优先、轻量、隐私友好、开源。

---

## 1. 一句话定位

一个常驻 Mac 菜单栏、到点从屏幕右侧滑入糖果色弹窗的提醒工具，**正在逐步演化为一个本地任务收件箱**——让脚本、构建任务、AI coding 工具把你真正想看到的事件，温和地投递到一个安静的本地入口。

起点（今天）：覆盖「每日任务 / 久坐 / 纪念日」三类提醒。
方向（往后）：成为长耗时 AI / 开发任务的本地「完成信号」收口。

## 2. 目标用户 & 场景

**起步用户：作者自己，以及任何想要一个安静本地提醒/任务通知入口的开发者。**

| 阶段 | 场景 | 触发来源 | 例子 |
|------|------|---------|------|
| 现在 | 每日固定事务 | daily | 14:00 写周报 |
| 现在 | 久坐健康 | interval | 每 60 分钟起身 |
| 现在 | 重要日子 | anniversary | 每年 06-15 |
| 往后 | 脚本/构建完成 | CLI `macabell notify` | 测试跑完、构建成功/失败 |
| 往后 | AI coding 任务完成 | Codex / Claude Code / Cursor 钩子 | 长任务跑完、需要你回来确认 |

设计取向：**情绪友好 + 不打断心流**——不是冷冰冰的系统通知，而是看着舒服、不焦虑（不抢焦点、可随手点掉、多条自动堆叠不覆盖）。这一点在「开发者长任务通知」场景同样成立：你要的是一个安静、不打断、可回头翻看的入口，而不是又一个抢注意力的弹窗轰炸器。

## 3. 当前功能（v0.2 已实现）

- ✅ 菜单栏常驻，无主窗口；托盘菜单：测试提醒 / 打开配置文件 / Finder 中显示 / 声音开关 / 开机自启 / 退出
- ✅ 三类提醒调度（daily / interval / anniversary），后台每 20s 检查
- ✅ 右侧滑入弹窗，**常驻不自动消失**，手动点掉；多条同时触发自动堆叠、互不覆盖、可单独关闭
- ✅ 糖果色马卡龙风格，6 套配色每次随机
- ✅ 提示音，带全局开关
- ✅ 开机自启（托盘可开关，基于 LaunchAgent）
- ✅ 配置即文件：`~/.MacaBell/reminders.json`
- ✅ 配置热加载：改任意字段 ≤20s 自动生效，无需重启（文件为单一事实源）
- ✅ 配置出错时温和提示，修好后自动恢复
- ✅ Universal DMG（Apple Silicon + Intel）+ GitHub Actions 自动打包

技术栈：Tauri 2 + React + TS。后端 `src-tauri/src/lib.rs`，UI `src/App.tsx` + `App.css`。

## 4. 赛道现状与定位差异（2026-06 核查）

提醒/休息提醒赛道极度拥挤，单纯「可爱」差异点已被占（如 LookAway 占据「可爱粉嫩」生态位，系统「提醒事项 + 专注模式」覆盖大部分任务/纪念日需求）。

**所以 MacaBell 不在「又一个更可爱的休息提醒」这条红海里竞争。** 它的差异化定位是另一条线：

| 维度 | MacaBell 的取向 | 说明 |
|------|----------------|------|
| 本地优先 / 纯文件配置 | 无账号、无云、单一 JSON | 隐私友好，开发者一眼读懂可 fork |
| 不打断心流的投递体验 | 不抢焦点、可堆叠、可回看 | 适配「长任务完成信号」场景 |
| 面向 AI coding workflow | CLI + 钩子把开发/AI 事件收口到本地 | **这才是别人没占的窄切口** |
| 代码足够小 | 任何开发者能读、能改、能当 Tauri menubar 起手式 | 复利资产 |

**判断**：在「更可爱的提醒」上没有护城河，所以不打那一仗；真正没被占的位置是「本地、安静、面向 AI/开发任务的通知收件箱」——这是把现有的「温和投递」能力，对准一个正在快速增长的真实痛点（长耗时 AI 任务缺一个不打扰的本地完成信号）。

## 5. 演化逻辑：为什么是「提醒 → 本地收件箱」

这条演化不是功能堆叠，而是同一个内核的延伸：

- **同一个投递层**：daily/interval/anniversary 和 CLI/钩子事件，最终都走同一套「温和弹窗 + 堆叠 + 本地记录」。新方向复用旧能力，不另起炉灶。
- **同一个价值观**：本地优先、不打断、单一数据文件、开源可读。从提醒到收件箱，价值观不变，只是事件来源变多。
- **同一类用户**：作者本人就是重度 AI coding 用户，自身就是首个真实需求方——这保证方向有真实的「自用校验」，不靠想象做产品。

> ⚠️ 自检红线（来自历史教训）：不要把「可爱审美」当护城河重演老剧本。新方向的赌注是**「本地 AI 任务通知是真需求」**，不是「界面更萌」。后续每个 AI 集成（§7 Future）**只在真实开发者觉得有用时才扩**——用真实使用而非想象来裁决。

## 6. 验证策略（可逆、低成本、用数据裁决）

新方向是一次方向性押注，按可逆方式推进，而不是一次性重投：

| 阶段 | 最小验证动作 | 看什么信号 |
|------|------------|-----------|
| v0.3 CLI | 自己把 `macabell notify` 接进日常脚本/构建 | 作者是否真的天天用、是否减少了盯终端 |
| v0.4 收件箱 | 单一本地数据文件 + 简单历史窗口 | 是否真的会回头翻、状态是否帮上忙 |
| Future 集成 | 先做 1 个集成（如 Claude Code 钩子）发出去 | 有没有外部开发者主动用/反馈 |

每一步都保留 kill switch：**没有真实使用信号就不往下扩**。先发免费开源版看自然留存与反馈，而不是一上来打磨变现。

## 7. Roadmap

> 与 `ROADMAP.md` 对齐。整体保持本地优先、轻量、隐私友好、开源。

### v0.2.0 — 更易安装与使用（✅ 已完成）
- Universal macOS 构建（Apple Silicon + Intel）
- GitHub Actions 发布构建流水线
- 托盘直接打开 `~/.MacaBell/reminders.json`
- 托盘在 Finder 中显示配置目录
- JSON 配置出错时的友好提示
- 多条提醒同时触发的堆叠
- 更清晰的截图与示例 `reminders.json`

### v0.3.0 — 通过 CLI 接收本地任务通知
目标：让开发工具与脚本能把事件投递进 MacaBell。
- 新增 `macabell notify` 命令行接口
- 支持 `--source / --project / --status / --title / --message` 参数
- CLI 事件复用同一套温和弹窗系统
- 文档化如何接入 Shell 脚本与构建步骤
- 全程本地——无网络、无账号

### v0.4.0 — 本地任务收件箱
目标：把一次性通知变成可浏览的本地收件箱。
- 一个简单的任务收件箱窗口，带事件历史
- 任务状态（pending / done / failed）
- 轻量动作：打开项目 / 复制消息 / 关闭
- 按来源与项目过滤、分组
- 仍是单一本地数据文件，仍然易懂

### Future — AI coding workflow 集成
目标：成为长耗时 AI / 开发任务的本地「完成信号」。
- Codex、Claude Code、Cursor 的集成
- Shell 脚本、测试、构建、部署的钩子
- 这些**只在真实开发者觉得有用时才扩**

### Later ideas（次要，除非有真实需求）
- 自定义主题、更多重复规则、配置导入导出
- 可选签名+公证构建、可选自动更新
- 抽出独立的 Tauri 2 menubar 起手模板

## 8. 成功标准

- **作为提醒工具（当前）**：作者每天实际用它、靠它没漏掉久坐/任务/纪念日。
- **作为本地收件箱（v0.3+）**：作者把 AI/构建任务接进来后，真的减少了盯终端、回头会翻收件箱。
- **作为开源项目/脚手架**：下一个 Mac 小工具能 1 小时内基于它起步；有外部开发者 fork 或反馈。

## 9. 非目标（明确不做）

MacaBell 不应变成一个重型 SaaS 生产力平台。明确不做：

- 账号系统
- 云同步
- 团队协作
- 完整日历替代品

也不做：

- 跨平台（Windows/Linux）—— 起步只在 Mac
- 健康数据统计/会议感知 —— 那是 Restier/DeskRest 的战场，不入局
- 变现 —— 除非 §6 的验证信号被证实

> 核心价值不变：一个安静、本地、易懂的工具——先是提醒，再逐步成为面向开发者与 AI coding workflow 的本地收件箱。
